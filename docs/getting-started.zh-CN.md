# 上手——从一条安装命令，到落在你自己分支上的一次合并

> **给第一次安装它的人。** 这一份把一整个闭环走完，一步不跳：装二进制、建一座城、接一个 provider、把想法交给 Mayor、看着各栋楼干活、读改动，最后读落在你分支上的那次合并。
>
> 它不解释词汇（[`glossary.md`](glossary.md)），不讲日常操作（[`operating.md`](operating.md)），也不讲设计（[`../ARCHITECTURE.md`](../ARCHITECTURE.md)）。英文版是 [`getting-started.md`](getting-started.md)。

## 你需要什么

一个桌面浏览器，以及一个能调用的模型——一个说 OpenAI 或 Anthropic 兼容格式的 provider 的 API key、一个能登录的订阅，或者一个说这两种兼容格式之一的本地服务。

别的都不需要：不装 npm，不装 node，不装语言运行时，不装数据库。

## 1 一条命令装好

macOS 或 Linux：

```sh
curl -fsSL https://raw.githubusercontent.com/2youg1/sprawling/main/install.sh | sh
```

Windows PowerShell：

```powershell
irm https://raw.githubusercontent.com/2youg1/sprawling/main/install.ps1 | iex
```

脚本取回适配你这个平台的最新发布包并解压；二进制最终装在哪、`PATH` 怎么改，由脚本最后调用的 `sprawling install` 决定，而这条命令你以后随时可以再跑一次。发布流水线没有构建的平台，它会连同已构建平台的清单一起报出来，而不是猜一个。

这些二进制没有代码签名，所以第一次运行会触发警告：Windows 说「Windows protected your PC」，走 *More info*、再 *Run anyway*；macOS 会拒绝第一次启动，在 Finder 里右键打开一次即可。

**不要用 `cargo install` 装它。** 客户端是 WebAssembly，先构建、再嵌进二进制；只跑一次 cargo build 得到的二进制，页面是空白的。要自己构建就读 [`CONTRIBUTING.md`](CONTRIBUTING.md)，并用 `just dist`。

## 2 建一座城，并把它打开

```bash
sprawling up ~/cities/first
```

这一条在目录里还没有城时把城建起来，服务在 `127.0.0.1:8787` 上，并打开页面。一座城就是一个目录，一切都在它里面：创世记录（城名也存在那里），以及账本（这座城的全部历史）。

**每一座城建起来时就已经有一栋楼**：`hall`，即 City Hall，它自己不持有任何项目。里面住着两位常驻。

- `hall/mayor`——Mayor，这座城的规划者。它只写 Markdown：没有 `exec`、没有 `delegate`、没有 `workshop`，因为一个能跑代码的规划者会停止阅读各栋楼给出的证据，转而生产自己的证据。
- `hall/clerk`——clerk，替你回答你已委托出去的审批，用 Gate 同样的三段式，理由写进账本。

这两位各自是谁，写在 `<city>/.sprawling/MAYOR.md` 与 `<city>/.sprawling/CLERK.md`。那棵子树在每一个写域之外，所以 Mayor 改不了「Mayor 是谁」。两个文件都由 [`templates/`](templates/) 里的模板铺下，且恒不覆盖已有内容，你写下的东西不会丢。**这个构建没有任何页面能编辑这两个文件，请用你自己的编辑器改。**

要让同一网络里的另一台机器连进来，先给一个要绑的地址并设配对令牌：

```bash
SPRAWLING_PAIRING_TOKEN=<你自己定的令牌> sprawling serve ~/cities/first 0.0.0.0:8787
```

绑非回环地址而没有令牌，它拒绝启动。这是绑定时刻的判定，不是请求时刻的：一座起得来却谁都拒的城看上去像网络故障，而一座起得来且谁都收的城更糟。

## 3 接一个 provider

在左栏打开 **设置**，填 **接一个 provider** 这一节。

| 字段 | 它要什么 |
|---|---|
| **叫它** | 你以后一眼能认出的名字 |
| **base URL（接口地址）** | provider 自己文档里给的 base URL。`https` 到哪都行，`http` 只能到本机 |
| **它说哪种线格式** | OpenAI 兼容格式，还是 Anthropic 兼容格式 |
| **密钥** | provider 的 key；本地服务留空 |

**密钥不会成为一条命令、也不会成为一个帧的一部分。** **把密钥放进保险库** 让它走自己的路进操作系统的凭证服务，回到页面上的只有一个 `secret:realm/name` 形式的引用；此后出现在配置、事件、日志里的都是这个引用。然后按 **接上这个 provider**，城会去问这个 provider 它提供哪些模型。

如果你是按订阅付费而不是按 token 付费，就用同一页上的 **用订阅登录**：**开始登录** 给你一个要打开的 URL，你在浏览器里批准，把 provider 显示的码贴回来，**完成登录** 把令牌连同它的端点一起放进凭证服务，并在过期之前续期——不是等一次调用被拒之后才续。今天这张表里只有 Anthropic 那一行是全的；某一行是空的 provider 会拒绝发起，而不是把你送进一个空页面。

最后，在 **给一个活选模型** 里用 **把这个活指向那个模型** 给每个活指一个模型。开头有两个活要紧：

- `main`——负责思考的模型。没有它，派活会被拒。
- `digest`——替 `main` 读长文档的模型。

两个可以是同一个模型。上下文窗口和输出上限不用你填：那是模型的事实，城从刚取回的清单里读。

## 4 把想法交给 Mayor

去 **在做的事**。顶上那个框只要一句话；下面那行灰字说这句话送去哪、以什么方式跑——带点线的词是这座城猜的，实线的是你自己设的，点任意一个词都能改。

1. 写下想法。一句就够：*把账本读取重写一遍，让一百万条事件的冷读稳定在一秒以内，并写下你测出来的数。*
2. 点那个房间词，输入 `hall/mayor`。斜杠前是楼，斜杠后是会话名，所以这一步在 City Hall 里开一个叫 `mayor` 的会话。下拉给出的是这座城已有的楼；房间是唯一允许你自由输入的字段。
3. 模式词保持 `build`，这是先出计划再干活的那一个。
4. 按 **派出去**，或者按 Enter。

**这个框不问预算。** 一件活跑之前没人说得出它值多少钱，订阅更是没有单价，花了多少事后从记录里报出来。要停下一个不该继续的 Run，就取消它；要停下全部，用 **停城**。

Mayor 拿到这个想法之后做的事：

- 先读再写——每一栋楼的 `Roadmap.md`、`Memo.md`、`Handoff.md`，以及有 `SPEC.md` 的楼的 `SPEC.md`。
- 用它的 `plan` 工具把城的计划写进 `<city>/hall/Roadmap.md`，一行一条工作线，权重按它对你值多少来定。
- 没有哪栋现成的楼该接这件事时，它盖一栋；你指给它的目录，它收编。一个项目它恒不盖两栋楼。
- 把各自那一份交给各栋楼，然后让那栋楼持续追下去。
- 在回报之前，把它决定了什么、为什么，记进 `<city>/hall/Memo.md`。

## 5 看着这座城干活

**在做的事** 列出在跑的会话和停下来等你的会话，已经结束的在下面一节。点开一个就是这个会话自己的页面，五个标签：**轮次** 是发生了什么，**改动** 是盘上什么动了，**花费** 是花了多少，**文档** 打开它所在的那栋楼，**提示词** 是发给模型的原文。

那一页也可以从城市图进去。它显示 **计划**，画的是这栋楼的 `Roadmap.md`，自己不存任何状态：一行是 **就绪**、**等依赖**、**在做**、**卡住** 或 **完成**，且只有叶子计入完成度——一个分支的活就是它的孩子，两边都数会把同一份力气数两遍。另一半是 **持续目标** 面板：**开始追** 让这栋楼自己不断派出就绪的活，就绪集空且无在途时停下。

**等我的** 是所有非等人回答不可的事。新城建起来时审批默认委托给 clerk，所以多数条目在那里就被回答了、理由落进账本；被 Policy 标为你自己答的，以及带着城外内容的条目，留给你。

**记录** 是同一段历史的三个透镜——**账本**、**归档**、**回收站**；**成本** 是花了多少，切成五个各自求和都等于同一个总数的维度。provider 没报价的那些，页面报 token 数并说明为什么没有金额，而不是打印 `$0.00`。

## 6 读改动，读那次落地的合并

要落地的活在城内走 pull request，而其中最要紧的一条不是靠谁记住的规矩：**写这份活的常驻，不能合并它自己的活。** 一个还没被验证过的请求，根本没有「合并」这个方法，所以未经复核的合并是一个编译错误，而不是一条制度。验证与合并是同一个动作，因为一个「已验证但没人合」的请求，会变成第三种要你去追的状态。

你按这个顺序读：

1. 会话的 **改动** 标签：动过的文件一行一个、按路径序排，点开一行给出那一个文件的补丁。被凭证扫描命中的行只报行号与理由，不回显原文——把字节打印出来证明泄漏，本身就是泄漏。
2. 那栋楼里的 `git log`。能进到分支上的只有两样：一个原本没有任何提交的仓库的首个提交，以及落下一份已复核工作的那次合并。城在每一波工具调用前写的围栏是没有任何 `HEAD` 指向的提交，挂在 `refs/sprawling/runs/` 下，所以你自己的历史仍是你离开时的形状。
3. 合并提交的 trailer，`git interpret-trailers --parse` 不需要我们帮忙就读得出来：`Sprawling-Run`、`Sprawling-Actor`、`Sprawling-Model`、`Sprawling-Effort`、`Sprawling-City`；当这个仓库的 git config 里有 `user.name` 与 `user.email` 时，还多一条 `Reviewed-by`。城不会替人编一个名字。
4. 同一个问题反过来问：`sprawling whose ~/cities/first <commit>` 答出哪一个 Run 写了这个提交，答案取自账本而不是 git，所以一座导出到另一台机器、旁边没有 `.git` 的城照样答得出。退出码 1 表示这座城没有写过那个提交的记录——这和「什么都没改」是不同的答案。

**这个构建没有替你合并的按钮，也没有推翻一次已合并工作的按钮。** 你的退路是 git（城做过的每一个提交都在里面）和 **回收站**（每一行都写着那个文件怎么取回来）；你的刹车是 **停城**，它让那个范围不再接新活。

## 7 停下，再开始

在服务端按 `Ctrl-C`，或者在页面上按 **停城**——后者会停下每一个 Run，并且这件事本身也被记录。

重启之后：

```bash
sprawling resume ~/cities/first
```

这一步验链、关掉那些「进程死时结果已丢失」的工具调用、报出谁在等人。一座在调用途中被杀掉的城，回来时说的是它不知道什么，而不是假装知道。

## 出了问题

| 你看到 | 它是什么意思 |
|---|---|
| 页面说 schema 不匹配 | 二进制和浏览器里的页面不是同一个版本。刷新。 |
| 接上之后模型列表是空的 | provider 答了，但答的内容这个构建读不出来。拒绝信息里写着它收到了什么。 |
| 还没开跑，派活就被拒 | 没有模型回答 `main`。**设置** 页顶部会直说这一点。 |
| 一个 Run 停下来要东西 | 它在 **等我的** 上，按簇分组，一个回答覆盖一个问题。 |
| **计划** 上有一行是 **卡住** | 它依赖的东西还没完成，或者某项检查没过。那一行写着它在等什么。 |
| 一次合并被拒 | 这份活分叉之后，主干动过了。那栋楼会把活重建在当前主干上，再验一次。 |
| 一个文件不见了 | 去 **回收站**。每一行都写着怎么把那个文件取回来。 |

更多这类情况和各自的处置在 [`operating.md`](operating.md)（英文）。
