[package]
name = "kernel"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
uuid.workspace = true
blake3.workspace = true
secrecy.workspace = true
zeroize.workspace = true
schemars = { workspace = true, optional = true }

[lints]
workspace = true

[dev-dependencies]
proptest.workspace = true
insta.workspace = true
trybuild.workspace = true

[features]
conformance = []
# The JSON Schema of every value that crosses the wire, derived from the
# same declarations serde reads (kernel-SPEC.md section 8-45). Off by
# default: the product binary never asks what its own frames look like.
schema = ["dep:schemars"]
